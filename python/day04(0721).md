## 보충

- print(’happy’, end = ‘,‘)
    
    ```python
    print(’happy’, end = ',')
    # h, a, p, p, y, (주의 : 마지막 출력값에도 end값이 들어간다)
    ```
    
    - end :  출력값 뒤에 어떻게 처리할지, 기본값은 \n (한줄띄기)
        - 주의 : 마지막 출력값에도 end값이 들어간다.